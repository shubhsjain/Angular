import { NewDDirective } from './new-d.directive';

describe('NewDDirective', () => {
  it('should create an instance', () => {
    const directive = new NewDDirective();
    expect(directive).toBeTruthy();
  });
});
